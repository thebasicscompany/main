{
  "name": "basics-runtime",
  "private": true,
  "version": "0.0.0",
  "description": "Basics Runtime — cloud workflow runtime + voice infra",
  "type": "module",
  "scripts": {
    "dev": "doppler run --project backend --config dev -- pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "db:generate": "doppler run --project backend --config dev -- pnpm --filter @basics/api db:generate",
    "db:migrate": "doppler run --project backend --config dev -- pnpm --filter @basics/api db:migrate",
    "db:migrate:railway": "railway run -- pnpm --filter @basics/api db:migrate",
    "e2e:live": "doppler run --project backend --config dev -- pnpm --filter @basics/api e2e:live",
    "e2e:live:railway": "railway run -- pnpm --filter @basics/api e2e:live",
    "dev:sst": "sst dev",
    "build:sst": "sst build",
    "deploy": "sst deploy --stage production"
  },
  "devDependencies": {
    "sst": "^3.0.0",
    "tsx": "^4.15.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "pnpm": {
    "overrides": {
      "zod": "4.3.6"
    }
  },
  "packageManager": "pnpm@10.33.0"
}
